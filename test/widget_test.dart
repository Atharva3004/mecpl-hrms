// Basic Flutter widget test for MECPL HRMS
import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App builds successfully', (WidgetTester tester) async {
    // Basic test to verify the app can be built
    // Full widget tests can be added as the app grows
    expect(true, isTrue);
  });
}
